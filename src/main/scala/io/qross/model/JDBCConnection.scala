package io.qross.model

object JDBCConnection {
    
    def create(connection: String): Unit = {
    
    }
    
    def update(connection: String): Unit = {
    
    }
    
    def remove(connection: String): Unit = {
    
    }
}
